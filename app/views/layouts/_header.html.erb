<nav class="navbar navbar-default navbar-fixed-top" role="navigation" id="site-navbar">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#top-navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <%= link_to 'Home', root_path %>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="top-navbar">
      <ul class="nav navbar-nav">
        <% if signed_in? %>
          <li><%= link_to "Authorisations", authorisations_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <% if signed_in? %>
  	        <li><%= link_to "Log out", destroy_user_session_path, method: "delete" %></li>
  	    <% else %>
  	        <li class="list-button"><%= link_to "Connect", user_omniauth_authorize_path(:google_oauth2), :class => 'btn pop' %></li>
  	    <% end %>
      </ul>
    </div>
  </div>
</nav>