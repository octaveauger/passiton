<nav class="navbar navbar-default navbar-fixed-top" role="navigation" id="site-navbar">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#top-navbar">
          <span class="sr-only">Toggle</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="top-navbar">
      <ul class="nav navbar-nav">
        <% if admin_signed_in? %>
          <li><%= link_to 'Users', admin_users_path %></li>
          <li><%= link_to 'Authorisations', admin_authorisations_path %></li>
          <li><%= link_to 'Delegations', admin_delegations_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <% if admin_signed_in? %>
  	        <li><%= link_to 'Logout', admin_session_path(current_admin), method: 'delete' %></li>
  	    <% end %>
      </ul>
    </div>
  </div>
</nav>