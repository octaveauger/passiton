$('#results').append("<%= j render partial: 'admin/authorisations/authorisation', collection: @authorisations %>");
<% if @authorisations.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @authorisations %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
initialize();