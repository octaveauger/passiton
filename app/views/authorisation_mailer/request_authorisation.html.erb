<h1>Context request</h1>
<p>
	<%= @authorisation.requester.full_identity %> would like to get context on "<%= @authorisation.scope %>".
</p>
<% if !@authorisation.description.empty? %>
	<p>
		<strong>Here's a note from <%= @authorisation.requester.first_name %>:</strong><br/>
		<%= @authorisation.description %>
	</p>
<% end %>
<p>
	You can decide if you want to share emails related to <%= @authorisation.scope %> by following this link:
</p>
<div class="btn btn-primary"><%= link_to 'Review context request', authorisation_grant_url %></div>
<p>
  If you have any questions about this request, please contact <%= @authorisation.requester.first_name %>.
</p>
<%= render partial: 'boilerplate/email_footer' %>

